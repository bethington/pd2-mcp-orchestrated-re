# .env.local.template - Local development template
# Copy this to .env.local and customize for your local setup
# WARNING: .env.local should NEVER be committed to version control

# Local development session
SESSION_ID=local-dev-$(whoami)

# Local development secrets (generate new ones)
JWT_SECRET_KEY=local-dev-secret-$(openssl rand -hex 16)
API_SECRET_KEY=local-api-secret-$(openssl rand -hex 16)

# Local database URLs (if running externally)
# REDIS_URL=redis://localhost:6379
# DGRAPH_ENDPOINT=http://localhost:8080

# Local Wine/Game paths (if using local game files)
# GAME_FILES_PATH=/path/to/your/d2/installation
# WINE_DEBUG_LEVEL=+all

# Local development preferences
DEBUG_MODE=true
HOT_RELOAD=true
LOG_LEVEL=DEBUG

# Local port overrides (if default ports conflict)
# D2_VNC_PORT=5902
# MCP_COORDINATOR_PORT=8002
# WEB_DASHBOARD_PORT=82

# Local performance settings
WORKER_PROCESSES=2
MAX_MEMORY_USAGE=3G
CPU_LIMIT=2.0

# Personal development preferences
ENABLE_PROFILING=true
EXPOSE_DEBUG_PORTS=true